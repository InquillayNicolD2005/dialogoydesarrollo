# AJAX

Aquí se colocan los endpoints PHP que necesite el panel para operaciones asíncronas.

Los endpoints deben reutilizar `config/database.php` y validar permisos antes de modificar la base de datos.
